bucket_definitions:
  water_readings:
    # Optional: parameters to filter the query
    # parameters:
    #   - select: current_user_id()
    #     alias: user_id
    data:
      - SELECT * FROM water_readings